# CONTEXT.md — 企业应收账款台账系统 · 领域术语表

> 本文件只记录业务术语的准确含义，不含任何实现细节。术语在需求拷问会话中逐条敲定。

## 核心概念

### 台账记录（Ledger Entry）
一行台账 = **一个合同**。合同编号（contract_no）是台账记录的业务唯一标识：
- Excel 导入按合同编号判重（跳过 / 覆盖 / 允许重复）
- 同一合同下的多个项目不拆行，归入同一行；项目名称、工作性质、八大板块取该合同的代表值
- 财务对账（发票、回款）以合同为对象，与合同维度的台账天然对齐

## 组织

### 单位（债权单位）
应收账款的**债权主体**（法人），共 4 个：物化院、六勘院、测绘院、禹地公司。回答"这笔钱法律上是谁的应收"——决定按谁的名头对账。单位是**台账记录行上的字段**（每行各自标注），不是部门的上级组织。

### 部门
台账记录的**责任主体**（内部考核归属），30 个。回答"这个合同是哪个团队干的"——决定行级权限（谁能看/编辑）与清收考核归属。部门列表由管理员维护。

**部门与单位正交**：部门不隶属单位，同一部门可持有不同单位名头的合同；一笔台账 = 一个部门归属 × 一个单位债权。看板/汇总可分别按部门（责任口径）和按单位（法人口径）聚合。

### 个人独立核算归属
"翟悟飞""孙勇军"为**合法的台账归属主体**（挂靠项目/个人独立核算），以部门身份存在于部门列表，不是填错。权限与汇总口径同普通部门。

## 金额口径

### 决算金额
最终结算依据金额。业务惯例：**一般项目不专门做决算，直接以合同额替代**；只有按工作量结算的项目才在完工后单独定案。

### 合同额带入规则
导入/录入时，决算方式非"按工作量结算"的项目，**合同金额自动带入决算金额**（财务仍可手动修改）；仅工作量结算项目允许决算金额留空待定。台账以"决算金额"为唯一结算金额口径，不双轨存储。

### 含税口径
决算金额、开票金额、到账金额、核销金额及所有派生指标均按**含税价**计，同口径直接相加减。

### 决算未定时的计算规则
决算金额为空时：
- 账外应收（决算−开票）、应收余额（决算−到账−核销）显示为空（"—"），**不参与看板与汇总合计**
- 账内应收（开票−到账）照常计算显示（开票、到账均为实际发生额，不依赖决算）
- 财务补录决算金额后，相关列自动恢复计算

### 账内应收 / 账外应收 / 应收余额
- 账内应收 = 开票金额 − 到账金额
- 账外应收 = 决算金额 − 开票金额
- 应收余额 = 决算金额 − 到账金额 − 核销金额
- 三者均为派生指标，不作为台账存储字段

### 回款明细（新增）
与开票明细对称的逐笔回款登记：日期 / 金额 / 备注。到账金额 = 回款明细合计，只读联动。核销金额维持累计数不建明细（财务年终批量操作，逐笔价值低）。

### 开票明细
逐笔登记开票日期 / 发票号 / 金额；开票金额 = 开票明细合计。仅财务可维护。

## 角色与权限

### 三级角色
- **超级管理员**：全部能力，含用户/部门管理、系统设置
- **管理员**：财务人员。台账全部字段（含金额）、开票/回款明细、导入导出、选项管理
- **报账员**：实体部门账号。仅可编辑催收跟踪类字段（项目状态 / 决算方式 / 债权状态 / 清收责任人 / 最新催收时间 / 沟通方式 / 对方反馈 / 最新进展 / 下一步计划）与附件；金额等财务字段由数据库触发器强制保护（前端显示 🔒）
- 另有 7 项逐人权限（查看本部门 / 查看全部 / 新增 / 编辑 / 删除 / 导入 / 导出），行级可见性按部门归属

（已实现并经需求方默认，未再深挖）

## 债权与催收

### 债权状态
**人工维护的催收处置阶段**（正常催收 / 逾期 / 诉讼 / 和解……），不是系统推导值。业务背景：按合同约定的付款期限衡量，账上存续项目几乎全部逾期，自动判定无区分度；该字段的真实用途是让财务一眼看出**处在正常催收还是已进入诉讼/和解等处置程序**。选项由财务在「选项管理」维护。

### 逾期预警
不建系统自动逾期规则；"逾期预警"需求由债权状态的人工维护 + 看板按债权状态筛选/汇总来满足。

### 清收责任人
文本字段，人工填写，通常为部门内人员。与部门归属（行级权限主体）相互独立。

## 记录生命周期

### 未结 / 结清
不建物理归档，所有记录永久留在一张台账中。台账总览默认筛选"**未结**"（应收余额 ≠ 0），结清/核销完的行默认隐藏、可一键切换"全部"。

### 核销
**可逆的数值事件，不是终态标记**：核销后若再次回款，核销金额可下调，应收余额自动恢复计算。不引入"结案"状态字段（避免与债权状态语义冲突）。核销操作也不做审批留痕。
